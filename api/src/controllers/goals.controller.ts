export const createGoal = () => {
  return 'goals.controllers/createGoal needs implementation'
}

export const deleteGoal = (id: number) => {
  return `goals.controllers/deleteGoal${id} needs implementation`
}

export const getGoal = (id: number) => {
  return `goals.controllers/getGoal${id} needs implementation`
}

export const getGoals = () => {
  return 'goals.controllers/getGoals needs implementation'
}

export const updateGoal = (id: number) => {
  return `goals.controllers/updateGoal${id} needs implementation`
}
